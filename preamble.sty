\usepackage{import}
\usepackage{imakeidx} % Must load before hyperref else index isn't linked
\usepackage[acronym,toc]{glossaries}
\usepackage{hyperref}
\usepackage{fontspec}
\usepackage{tocbibind}
\usepackage{caption}
\usepackage[a4paper]{geometry}
\usepackage[table]{xcolor}

\setmainfont{Open Sans}

\makeindex[intoc]
\makeglossaries

% Alternating row colors for all tables
\let\defaulttabular\tabular % Backup
\let\enddefaulttabular\endtabular % Backup

\renewenvironment{tabular}[1][]{
    \rowcolors{1}{white}{gray!30}
    \defaulttabular
}{\enddefaulttabular}

\captionsetup{justification=raggedright,singlelinecheck=false}
